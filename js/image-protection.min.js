// Image protection - prevent right-click and drag
document.addEventListener('contextmenu',e=>{e.target.tagName==='IMG'&&e.preventDefault()});document.addEventListener('dragstart',e=>{e.target.tagName==='IMG'&&e.preventDefault()});